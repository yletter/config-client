spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:${config.port:8080}
      username: user
      password: userpassword
  config:
    import: configserver:http://user:userpassword@localhost:$8080